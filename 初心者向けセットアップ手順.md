# 初心者向けセットアップ手順

プログラミングの知識がなくても進められるように、クリックする場所と入力する文字をすべて書いています。
コマンドは**コピーして貼り付けるだけ**で大丈夫です。

- **所要時間：** 1〜2時間（そのうち半分は、クライアントの基本情報を書く時間です）
- **必要なもの：** パソコン（Mac または Windows）、Claudeの有料プラン（Pro以上）、インターネット

---

## 全体の流れ

| ステップ | やること | 時間 |
|---|---|---|
| 1 | 一式をパソコンに移す | 5分 |
| 2 | Obsidianを入れて、フォルダを開く | 10分 |
| 3 | Claude Codeを入れる | 10分 |
| 4 | Claude Codeを起動してログインする | 10分 |
| 5 | 1社目の準備をする | 30〜60分 |
| 6 | 分析エージェントだけで試す | 15分 |
| 7 | 月次サイクルを丸ごと回す | 30分 |
| 8 | 全クライアントを毎月回す | 1社30分前後 |

---

## ステップ1：一式をパソコンに移す

1. パソコンのブラウザで claude.ai を開き、このチャットを開く
2. `recreat-instagram-team.zip` をダウンロードする
3. ダウンロードしたzipを展開する
   - **Mac：** zipをダブルクリック
   - **Windows：** zipを右クリック →「すべて展開」
4. 展開してできた `recreat-instagram-team` フォルダを、分かりやすい場所に移す（例：「書類」フォルダの中）
5. フォルダ名は変えてOKです（例：`RECREAT運用`）

> **中身の確認：** フォルダを開いて `CLAUDE.md`、`10_クライアント`、`20_ナレッジ` などが見えればOKです。
> `.claude` というフォルダは「隠しフォルダ」なので、最初は見えません。見たいときは次の操作で表示できます。
> - **Mac：** Finderでそのフォルダを開いて `Command + Shift + .`（ピリオド）
> - **Windows：** エクスプローラー上部の「表示」→「表示」→「隠しファイル」にチェック

---

## ステップ2：Obsidianを入れて、フォルダを開く

Obsidianは「メモ帳の高機能版」です。エージェントが作ったファイルを読んだり直したりするのに使います。

1. ブラウザで obsidian.md を開き、ダウンロードしてインストールする（無料です）
2. Obsidianを起動する
3. 最初の画面で「フォルダを保管庫として開く」（英語表示なら「Open folder as vault」）を選ぶ
4. ステップ1で置いたフォルダを選ぶ
5. 左側にフォルダの一覧が出れば完了

> Obsidianでは、このフォルダ全体のことを「保管庫（Vault）」と呼びます。

---

## ステップ3：Claude Codeを入れる

Claude Codeは「ターミナル」という黒い（または白い）文字だけの画面で動くアプリです。見慣れない画面ですが、やることは貼り付けとEnterだけです。

### Macの場合

1. `Command + スペース` を押して「ターミナル」と入力し、Enterで開く
2. 次の1行をコピーして貼り付け、Enterを押す

```
curl -fsSL https://claude.ai/install.sh | bash
```

3. 文字がたくさん流れて止まったら完了。**ターミナルを一度閉じて、もう一度開く**
4. 確認のため、次を貼り付けてEnter

```
claude --version
```

数字（バージョン番号）が表示されれば成功です。

### Windowsの場合

1. スタートボタンを右クリック →「ターミナル」（または「Windows PowerShell」）を開く
2. 次の1行をコピーして貼り付け、Enterを押す（管理者として実行する必要はありません）

```
irm https://claude.ai/install.ps1 | iex
```

3. 止まったら完了。**ターミナルを一度閉じて、もう一度開く**（閉じないと次の確認で「見つかりません」と出ます）
4. 確認のため、次を貼り付けてEnter

```
claude --version
```

数字が表示されれば成功です。

> **うまくいかないとき：** 表示されたエラーの文章をそのままclaude.aiのチャットに貼り付けて「Claude Codeのインストールでこのエラーが出た」と聞けば、解決方法を案内できます。

---

## ステップ4：Claude Codeを起動してログインする

**ここが一番大事なポイントです。Claude Codeは「起動したフォルダ」の中で仕事をします。** 必ずステップ1のフォルダで起動してください。

### フォルダでターミナルを開く方法

**Mac：**
1. ターミナルを開く
2. `cd ` と入力する（cdのあとに半角スペースを1つ）
3. Finderからステップ1のフォルダを、ターミナルの画面にドラッグ＆ドロップする
4. Enterを押す

**Windows：**
1. エクスプローラーでステップ1のフォルダを開く
2. フォルダの中の何もないところで右クリック →「ターミナルで開く」

### 起動とログイン

1. 次を入力してEnter

```
claude
```

2. 最初だけ、画面の案内が出ます。上下キーで選んでEnterで決定します
   - 画面の色（テーマ）：好きなものでOK
   - ログイン方法：**Claudeのアカウント（サブスクリプション）** を選ぶ
3. ブラウザが開くので、いつものClaudeアカウントでログインして許可する
4. ターミナルに戻ると「このフォルダを信頼しますか？」と聞かれるので、**信頼する（Yes）** を選ぶ
5. 入力欄が出たら準備完了です

### 動作確認

入力欄に次のように打ってみてください。

```
このフォルダにあるエージェントの一覧を教えて
```

7体（production-director、competitor-researcher、instagram-analyst、ad-operator、content-planner、design-director、report-writer）の名前が出てくれば成功です。

> **終わるとき：** `/exit` と入力してEnter（または `Ctrl + C` を2回）

---

## ステップ5：1社目の準備をする

最初は、データがそろっていて、慣れているクライアント1社で試すのがおすすめです。以下は架空の「焼肉店A」で説明します。

### 5-1. フォルダを作る

Obsidianの左側で `10_クライアント` を右クリック →「新規フォルダ」→ 名前を `焼肉店A` にする

### 5-2. テンプレートをコピーする

1. `90_テンプレート` の `クライアント基本情報.md` を右クリック →「複製」
2. できたファイルを `焼肉店A` フォルダにドラッグし、名前を `基本情報` にする
3. 同じように `効いた型.md` も複製して `焼肉店A` フォルダに入れ、名前を `効いた型` にする

### 5-3. 基本情報を埋める（一番大事）

自分で書いてもいいですが、**Claude Codeに質問してもらいながら作る**のが楽です。ステップ4の方法で `claude` を起動し、こう頼みます。

```
10_クライアント/焼肉店A/基本情報.md を一緒に埋めたい。
テンプレートの項目を上から順に1つずつ質問して、答えた内容をファイルに書き込んで。
```

質問に答えていくだけで基本情報が完成します。

> **途中で「ファイルを編集していいですか？」と聞かれます。** 内容を確認して許可してください。「このセッション中は聞かない」系の選択肢を選ぶと、以降は聞かれなくなります。

### 5-4. データを置く

1. `焼肉店A` フォルダの中に `データ` フォルダを作る
2. Metaビジネススイートのインサイト画面から、先月分のデータを書き出してこのフォルダに入れる（ファイル名は `2026-09_インサイト.csv` のように月が分かる名前にしておくと安心です）
3. 広告を運用している場合は、広告マネージャから書き出したデータも同じフォルダに入れる

---

## ステップ6：分析エージェントだけで試す

いきなり全員を動かさず、まず1人だけ動かして様子を見ます。

1. ステップ4の方法でフォルダを開き、`claude` を起動
2. 次のように頼む

```
instagram-analyst エージェントを使って、焼肉店Aの9月の分析をして
```

3. 数分待つと「保存先」と「要点3行」が表示される
4. Obsidianで `10_クライアント/焼肉店A/分析/2026-09_分析.md` を開いて読む

### 確認するポイント

- 数字がCSVと合っているか
- 「なぜ伸びたか」の理由に納得できるか
- 自分ならこう書く、という違いがあるか

違いがあれば、そのままClaude Codeに伝えます。

```
分析のやり方をこう直したい：（直してほしい点）
instagram-analyst エージェントの定義ファイルに反映して
```

これを2〜3回くり返すと、自分好みの分析をしてくれるようになります。

---

## ステップ7：月次サイクルを丸ごと回す

分析に納得できたら、ディレクターに全体を任せます。

1. フォルダでターミナルを開き、今度は次のコマンドで起動する

```
claude --agent production-director
```

2. 次のように入力する（スラッシュ、monthly、半角スペース、クライアントのフォルダ名、半角スペース、対象月）

```
/monthly 焼肉店A 2026-09
```

3. ディレクターが作業リストを見せてくるので、問題なければ「OK」と返す
4. 各エージェントが順番に動く（20〜30分ほど。待っている間は別の作業をして大丈夫です）
5. 最後に「確認してほしいこと」が表示されるので、答える
6. Obsidianで `レポート/` のファイルを読み、必要なら直す
7. `送付/YYYY-MM_送付一式.md` を開く。送付状の文面と同梱物リストができているので、
   チェックリストを確認してからクライアントに送る

> **クライアントのフォルダ名にスペースを入れないでください。** `/monthly 焼肉店 A` のようにスペースがあると、2つの言葉として読まれてしまいます。

---

## ステップ8：全クライアントを毎月回す

### 月初にやること（人の作業）

1. 全クライアントのインサイトCSV・広告CSVを、それぞれの `データ/` フォルダにまとめて置く
2. 新しいクライアントがいれば、ステップ5で準備しておく

### 1社ずつ回す

1. `claude --agent production-director` で起動する
2. `/monthly 焼肉店A 2026-09` と入力する
3. 終わったら、確認事項に答える
4. **`/clear` と入力する**（会話がリセットされます。これを忘れると、前の会社の情報が次の会社に混ざる原因になります）
5. `/monthly 美容室B 2026-09` のように、次の会社を入力する
6. これを全社分くり返す

```
/monthly 焼肉店A 2026-09
（終わる）
/clear
/monthly 美容室B 2026-09
（終わる）
/clear
/monthly 工務店C 2026-09
```

### 進み具合を見る

Obsidianで `05_月次管理/2026-09.md` を開くと、全社の状態が一覧で見られます。

| クライアント | 状態 | 分析 | 広告 | 競合 | 企画 | デザイン | レポート | 送付 | 提出 |
|---|---|---|---|---|---|---|---|---|---|
| 焼肉店A | 確認待ち | 済 | 済 | 済 | 済 | 済 | 済 | 確認待ち | 未 |
| 美容室B | 作業中 | 済 | ― | 済 | 作業中 | 未 | 未 | 未 | 未 |
| 工務店C | 未 | 未 | 未 | 未 | 未 | 未 | 未 | 未 | 未 |

「確認待ち」の会社は、レポートを確認して提出する番です。提出したら、Obsidianで「提出」欄を「済」に書き換えます。

### 1日に回す社数の目安

- **Proプラン：** 1日2〜3社ずつ、数日に分けて回す
- **Max 5x：** 1日に全社回せることが多い。ターミナルを2つ開いて同時に2社まで
- 途中で利用上限に達したら、時間をおいて同じ `/monthly` を実行し直せば、管理表を見て続きから進めます

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| `claude` と打つと「見つかりません」と出る | インストール後にターミナルを開き直していない。一度閉じて開き直す |
| 「エージェントが見つからない」と言われる | 違うフォルダで起動している。ステップ4の方法でフォルダを開き直す |
| 「基本情報が未作成」と言われる | ファイル名かフォルダの場所が違う。`10_クライアント/<名前>/基本情報.md` になっているか確認 |
| 「データ未配置」と言われる | `データ` フォルダにCSVが入っていない |
| `/monthly` と打っても反応しない | 違うフォルダで起動している。または `.claude/skills/monthly/SKILL.md` の場所がずれている |
| 「/clear してから再実行して」と言われる | 同じ会話で別の会社を扱おうとした。`/clear` を入力してからやり直す |
| 利用上限に達したと出る | しばらく待つと回復する。頻繁に止まるならプランの見直しを検討 |
| 何を聞かれているか分からない | その画面の文章をclaude.aiのチャットに貼り付けて「これはどういう意味？」と聞く |

---

## 慣れてきたらやること

- **Windsor.aiをつなぐ：** CSVを手で置かなくても、エージェントがデータを取りに行けるようになります（`はじめかた.md` の手順2）
- **2社目を追加する：** ステップ5をくり返すだけです。月次管理表には次の `/monthly` 実行時に自動で行が追加されます
- **スキルを作る：** よく頼む作業を「この作業をスキルにして」とClaude Codeに頼むと、次から一言で呼べるようになります

---

## 用語メモ

| 用語 | 意味 |
|---|---|
| ターミナル | 文字を打ち込んでパソコンに指示する画面 |
| フォルダで起動 | そのフォルダの中身をClaude Codeの作業場所にすること |
| 保管庫（Vault） | Obsidianで開いているフォルダ全体のこと |
| エージェント | 役割ごとの指示書を持ったAIスタッフ。`.claude/agents/` にいる |
| CLAUDE.md | 全スタッフ共通の就業ルール |
| スキル | よく使う手順を `/monthly` のような短い命令で呼べるようにしたもの |
| /clear | 会話をリセットする命令。1社終わるごとに入力する |
| CSV | 表計算ソフトで開ける、数字の一覧ファイル |
| 送付一式 | クライアントに送る直前の原稿一式（送付状＋同梱物リスト＋チェックリスト） |
