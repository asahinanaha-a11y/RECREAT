# CLAUDE.md — 全エージェント共通の就業ルール

このフォルダは **Instagram運用代行チームの作業場（Obsidian保管庫）** です。
ここで働くすべてのエージェントは、以下のルールに従ってください。

---

## 1. 絶対に守ること

### 数値を作らない
- 数値は **`データ/` 内のファイル、または接続済みのデータソースから読み取った値だけ** を書く。
- 読み取れない指標は、推測せず `データなし` と書く。
- 「だいたい」「おそらく〇〇件程度」といった**推定値を数字として書かない**。
- 比較（前月比・前年比）は、両方の実データがある場合のみ計算する。片方しかなければ「前月データなし」と書く。

### クライアント情報を混ぜない
- 1つの会話で扱うクライアントは **常に1社だけ**。
- 会話の中に別クライアントの話題が既に出ている場合は、作業を始めずに
  「`/clear` してから実行してください」と伝えて止まる。
- 他社の数値・企画・表現を、別の会社の成果物に持ち込まない
  （横断的な学びは `20_ナレッジ/` を経由させ、社名や具体数値は落としてから使う）。

### 事実と解釈を分ける
- 成果物の中で「事実（データ）」と「解釈（仮説）」を必ず書き分ける。
- 解釈には「〜と考えられる」「〜が要因の可能性がある」と書き、断定しない。

### 人が最終確認する
- どのエージェントも、クライアントへの提出はしない。作るのは提出前の原稿まで。
- 判断に迷ったら勝手に決めず、成果物の末尾「確認してほしいこと」に列挙する。

---

## 2. フォルダ構成

```
CLAUDE.md                    このファイル（共通ルール）
ホーム.md                     入口
はじめかた.md                  導入手順（短い版）
初心者向けセットアップ手順.md    導入手順（詳しい版）

05_月次管理/
  YYYY-MM.md                 その月の全社の進捗表

10_クライアント/
  <クライアント名>/
    基本情報.md               店舗・商材・ターゲット・トンマナ・KPI
    効いた型.md               その会社で実際に伸びた投稿の型
    データ/                   人が置くCSV（インサイト・広告）
    分析/    YYYY-MM_分析.md
    広告/    YYYY-MM_広告.md
    リサーチ/ YYYY-MM_競合.md
    企画/    YYYY-MM_企画.md
    デザイン/ YYYY-MM_デザイン.md
    レポート/ YYYY-MM_レポート.md
    送付/    YYYY-MM_送付一式.md ＋ 実際に送るファイル

20_ナレッジ/                  全社横断の知見（社名・実数値は書かない）
90_テンプレート/               各成果物のひな形
99_添付/                      画像などの添付ファイル

.claude/agents/              エージェントの定義
.claude/skills/monthly/      /monthly コマンドの手順書
```

---

## 3. ファイルの書き方

### 命名規則
- 成果物：`YYYY-MM_<種別>.md`（例：`2026-09_分析.md`）
- 対象月は「データの月」。9月の実績なら、作業が10月でも `2026-09` を使う。
- クライアントのフォルダ名にスペースを入れない。

### frontmatter（ファイル冒頭のメタ情報）
すべての成果物に付ける。

```yaml
---
クライアント: 焼肉店A
対象月: 2026-09
種別: 分析
作成日: 2026-10-03
担当: instagram-analyst
状態: 下書き        # 下書き / 確認待ち / 済
データ元: データ/2026-09_インサイト.csv
---
```

### 文章のスタイル
- 日本語。です・ます調。
- 1文を短く。専門用語は初出で1行の説明を添える。
- 箇条書きを優先し、長い散文にしない。
- クライアントが読むもの（レポート）は、社内用語・略語を使わない。

---

## 4. 作業の型（すべてのエージェント共通）

1. **読む**：`基本情報.md` → `効いた型.md` → 対象月のデータ → 前月の同種成果物
2. **確認する**：必要なファイルが無ければ、作業せずに何が足りないかを報告する
3. **書く**：テンプレート（`90_テンプレート/`）の構成に沿って成果物を作る
4. **報告する**：以下の3点だけを短く返す
   - 保存先のパス
   - 要点3行
   - 確認してほしいこと（無ければ「なし」）

### 足りないときの止まり方
| 状況 | 返すメッセージ |
|---|---|
| 基本情報.md が無い | 「基本情報が未作成です。`10_クライアント/<名前>/基本情報.md` を作ってください」 |
| データ/ が空 | 「データ未配置です。`10_クライアント/<名前>/データ/` にCSVを置いてください」 |
| 対象月のデータが無い | 「対象月（YYYY-MM）のデータが見つかりません。ファイル名に月が入っているか確認してください」 |
| 前工程の成果物が無い | 「先に <種別> が必要です。`<エージェント名>` を先に動かしてください」 |

勝手に代替データで進めない。止まって報告する。

---

## 5. 月次管理表の更新

成果物を1つ作り終えるたびに `05_月次管理/YYYY-MM.md` の該当セルを更新する。

- 状態の値：`未` / `作業中` / `確認待ち` / `済` / `―`（対象外）
- 行が無ければ追加する
- 全工程が終わったら、その会社の「状態」列を `確認待ち` にする
- 「提出」列は**人だけが更新する**。エージェントは触らない
  （人から「〇〇は提出済み」と言われた場合のみ `済` に変更する）

### 送付データについて
- 「送付」列は、クライアントに送る一式が `送付/` にそろった時点で `済` にする。
- 送付一式に入れてよいのは、**人が確認済みの成果物だけ**。
  エージェントは一式を組み立て、送付状の文面まで作るが、送信はしない。

---

## 6. 広告・薬機法・景表法の注意

- 医療・美容・健康食品のクライアントでは、効果を断定する表現を使わない
  （例：「必ず痩せる」「シミが消える」「治る」）。
- 「No.1」「業界最安」などの優良誤認になりうる表現は、根拠資料がある場合のみ。
- 判断に迷う表現は使わず、「確認してほしいこと」に挙げる。
- 詳細は `20_ナレッジ/表現の注意点.md` を参照。

---

## 7. Windsor.ai（任意）

Windsor.aiのMCPが接続されている場合は、CSVの代わりに直接データを取得してよい。
その場合も frontmatter の `データ元` に、取得元と取得日を書き残すこと。
接続されていない場合は、必ず `データ/` のCSVを使う。
