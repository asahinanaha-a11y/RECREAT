---
種別: ガイド
---

# はじめかた（短い版）

パソコンの操作に慣れている方向けの手順です。
1つずつ画面を見ながら進めたい場合は [[初心者向けセットアップ手順]] を読んでください。

---

## 手順1：動かせる状態にする

1. このフォルダをObsidianで「フォルダを保管庫として開く」で開く
2. ターミナルでこのフォルダに移動し、Claude Codeを起動する

```
cd <このフォルダ>
claude
```

3. 動作確認

```
このフォルダにあるエージェントの一覧を教えて
```

7体（production-director / competitor-researcher / instagram-analyst /
ad-operator / content-planner / design-director / report-writer）が出れば準備完了です。

---

## 手順2：Windsor.aiをつなぐ（任意）

CSVを手で書き出す代わりに、エージェントが直接データを取りに行けるようになります。
接続しない場合は、`データ/` にCSVを置く運用のままで問題ありません。

1. windsor.ai でアカウントを作り、Instagram（Meta）アカウントを接続する
2. Claude CodeにWindsor.aiのMCPサーバーを追加する

```
claude mcp add --transport http windsor <Windsor.aiが発行するMCPのURL>
```

3. Claude Codeを再起動し、確認する

```
Windsorで取得できるコネクタの一覧を出して
```

4. つながったら、分析時にこう頼めます

```
Windsorから焼肉店Aの2026年9月のInstagramデータを取得して、そのまま分析して
```

> 接続していても、frontmatter の `データ元` に取得元と取得日を必ず残します。

---

## 手順3：1社目を用意する

```
10_クライアント/焼肉店A/
  基本情報.md     ← 90_テンプレート/クライアント基本情報.md をコピー
  効いた型.md     ← 90_テンプレート/効いた型.md をコピー
  データ/         ← インサイトCSV・広告CSVを置く
```

基本情報は、Claude Codeに聞いてもらいながら埋めるのが早いです。

```
10_クライアント/焼肉店A/基本情報.md を一緒に埋めたい。
テンプレートの項目を上から順に1つずつ質問して、答えた内容をファイルに書き込んで。
```

**基本情報の質が、そのまま成果物の質になります。** ここだけは手を抜かないでください。

---

## 手順4：まず1エージェントだけ試す

```
instagram-analyst エージェントを使って、焼肉店Aの2026年9月の分析をして
```

出てきた分析を読み、自分の見立てと違うところがあれば、そのまま伝えて直させます。

```
分析のやり方をこう直したい：（直してほしい点）
instagram-analyst エージェントの定義ファイルに反映して
```

2〜3回くり返すと、自分の型に近づきます。

---

## 手順5：月次サイクルを丸ごと回す

```
claude --agent production-director
```

を起動して、

```
/monthly 焼肉店A 2026-09
```

分析 → 広告 → 競合 → 企画 → デザイン → レポート → 送付一式 まで一気に作ります。
所要20〜30分。終わったら `送付/` の送付一式を確認して、クライアントに送ります。

**次の会社に移る前に必ず `/clear`。** 情報の混線を防ぐためです。

---

## 毎月の流れ

| タイミング | やること | 誰が |
|---|---|---|
| 月初 | 各クライアントの `データ/` にCSVを置く | 人 |
| 月初 | `/monthly <会社> <月>` を1社ずつ実行 | Claude Code |
| 実行後 | レポートと送付一式を確認・修正 | 人 |
| 送付後 | 「〇〇は提出済み」と伝える | 人 |
| 随時 | `05_月次管理/YYYY-MM.md` で進捗を確認 | 人 |

---

## 困ったとき

- [[初心者向けセットアップ手順]] の「よくあるつまずき」を見る
- エラー文をそのままClaude Codeに貼って「これはどういう意味？」と聞く
